export const FETCH_USERS_REQUEST='FETCH_USERS_REQUEST'
import {}
const initialState={
    loading:false,
    users:[],
    error:''
}
const reducer=(state=initialState,action)=>{
switch(action.type){
  case 
}
}